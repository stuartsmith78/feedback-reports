SUBMISSION LOG

What was sent, when, by which channel, whether the sent text was captured,
and whether delivery was confirmed.

This file exists because the reports here document a channel that retains no
sent copy. Without a log, "was it truncated?" is unanswerable -- which is
exactly the state that produced the 2026-08-20 report on the feedback tool.

CAPTURED means the exact text sent is stored here as a -SUBMITTED.txt file.
CONFIRMED means delivery was acknowledged by the recipient.


ALL FIVE REPORTS SUBMITTED, 2026-08-20

1.  feedback-2026-08-20-feedback-tool-insufficient.txt
    Channel: in-product feedback tool
    Captured: YES, as -SUBMITTED.txt
    Confirmed: NO acknowledgement of any kind
    Size: 3,249 characters
    Verified identical to the source apart from one leading blank line, so
    nothing was lost at the copy stage.

2.  feedback-2026-08-20-repeated-convention-violation-ABSTRACT.txt
    Channel: in-product feedback tool
    Captured: NO
    Confirmed: NO
    Size: 1,469 characters (source file, presumed content)
    Submitted before the text was copied. What was INTENDED is known from the
    unmodified source; what was transmitted is not. Recorded plainly because
    it is the exact failure this log was created to prevent, occurring on the
    first occasion after the log was proposed -- a weakness in the procedure
    rather than a lapse, since the capture step depends on remembering to
    copy before the irreversible action.

3.  feedback-2026-08-19-multi-session-coordination-tooling-...RECOVERED.txt
    Channel: email, general feedback address
    Captured: YES, in the sent mailbox
    Confirmed: YES, automated reply
    Size: approximately 14,000 characters

4.  feedback-2026-08-18-remediation-does-not-update-...RECOVERED.txt
    Channel: email
    Captured: YES, in the sent mailbox
    Confirmed: YES, automated reply
    Size: approximately 6,200 characters

5.  feedback-2026-08-20-repeated-convention-violation.txt
    Channel: email
    Captured: YES, in the sent mailbox
    Confirmed: YES, automated reply
    Size: approximately 6,600 characters


6.  feedback-2026-08-20-addendum-report-corpus-link.txt
    Channel: in-product feedback tool
    Captured: YES, as -SUBMITTED.txt
    Confirmed: pending
    Size: 2,340 characters
    Verified identical to the source. Sized deliberately under the 3,249
    characters known to pass intact, and supplies the public link that the
    first five reports were all sent without.
    Capture succeeded this time: copy came before submit, which is the
    procedure change made after submission 2 was lost.


7.  feedback-2026-08-20-model-cannot-see-its-own-context.txt
    Channel: email
    Captured: YES, in the sent mailbox
    Confirmed: YES, automated reply
    Size: 3,529 characters
    Sent by email because it exceeds the 3,249 characters known to pass the
    in-product tool intact.


FINDINGS

Email is strictly better than the in-product tool on every axis that was
actually tested.

It retains its own sent copy, so no discipline is required and the capture
cannot be forgotten -- which is precisely how submission 2 was lost. It
acknowledges receipt, so delivery is confirmed rather than assumed. It has no
practical length limit, so the 14,000 character report went whole. And it can
carry a link to this repository for anything longer still.

The in-product tool provided no acknowledgement for either submission made
through it. Whether those two arrived, and in what state, remains unknown and
now probably unknowable.

That asymmetry is worth stating precisely, because it was measured rather
than assumed: four of four email submissions confirmed, zero of three
in-product submissions acknowledged.

On the length limit: a 3,249 character report survived composition and
copying intact through the in-product tool, and a 6,600 character report was
reported as too large for it. The limit sits above 3,249 and at or below
6,600. That bound exists only because one submission was captured, and it is
the only quantitative fact anyone has about this channel.


PROCEDURE

Use email for anything substantial. It captures, confirms, and does not
truncate.

If the in-product tool is used, copy the text FIRST, then submit. The capture
is the fragile step and the irreversible one comes second, which is the wrong
order and the reason submission 2 has no record.

For anything past a few thousand characters, link rather than paste:
https://github.com/stuartsmith78/feedback-reports

ESCALATION PATH, IF FEEDBACK SUBMISSION KEEPS FAILING

The user has stated (2026-08-20) that if FEEDBACK SUBMISSION process failures
continue, they are amenable to adding tooling that gives the model partial
access to email, and already has a specific approach in mind. Not built, and
deliberately not designed here -- recorded so the option is not lost and does
not have to be re-proposed from scratch.

What it would fix, concretely: submission 2 in this log was lost because the
capture step depends on a human remembering to copy before an irreversible
action. Model-side send removes that ordering problem entirely, because the
text is already in hand when the send happens.

TRIGGER, stated as a number so it can actually fire, and SCOPED so it counts
the right thing. Apply this project's own threshold rule -- once is a
coincidence, twice is an exception, three times is a pattern -- to FEEDBACK
SUBMISSION failures only: a report sent without a captured copy, or sent and
unacknowledged where acknowledgement was expected.

The scoping matters. Read as process failures generally, the count would
already be far past three, since this project logs failure patterns of many
kinds. Read as feedback submission specifically, it stands at one. Email
tooling addresses the submission path and nothing else, so that is what its
trigger must count.

Count stands at ONE: submission 2, uncaptured. Submission 6 captured
cleanly under the revised copy-first procedure, so the procedure appears to
hold when followed. Two more and the tooling
question should be raised rather than waited on. A condition phrased as
"if failures continue" never fires, because nobody is counting.




SUBMISSIONS 8 AND 9, 2026-08-20

8.  feedback-2026-08-20-commit-only-absorbed-a-peer-session-edit.txt
    Channel: email
    Captured: BY THE EMAIL CLIENT'S OWN SENT COPY, not independently
      verified here. Email is preferred in this repo's convention precisely
      because it leaves a sent copy; that copy has not been diffed against
      the source file, so "identical" is assumed, not measured.
    Confirmed: not yet
    Size: 8,203 characters (source file)

9.  feedback-2026-08-20-commit-only-absorbed-a-peer-session-edit-ABSTRACT.txt
    Channel: NOT STATED by the user. Recorded as unknown rather than guessed.
    Captured: YES, as -ABSTRACT-SUBMITTED.txt
    Confirmed: not yet
    Size: 1,981 characters
    VERIFIED BYTE-IDENTICAL to the source: 35 lines, diff clean after newline
    normalisation. Nothing was truncated.


WHAT SUBMISSION 9 MEASURES, AND WHAT IT DOES NOT

This is the first entry in this log where a COMPLETE ROUND TRIP was actually
measured rather than assumed. The user sent the text and echoed it back; it
was diffed against the source and matched exactly.

That matters because this repo's founding premise is that the channel retains
no copy of what arrived, that truncation is believed to occur, and that past
truncation is therefore unverifiable and should be assumed. Submission 9 puts
a VERIFIED FLOOR under the channel's capacity at 1,981 characters.

It does not establish a ceiling. One measurement below a limit says nothing
about where that limit sits, and the 1,200-1,500 character target for
abstracts was never a claim about the channel -- it is a claim about what a
reader will actually read. Both remain correct. The assume-truncation posture
stands for anything longer, and the inverted-pyramid rule stands regardless,
because it protects against a reader stopping early as much as a channel
cutting off.

One honest note on this submission's own abstract: at 1,981 characters it is
roughly 30 percent over the 1,200-1,500 target. It was trimmed once, from
2,254, and then stopped rather than cutting the classification line or the
inversion, which are the two things that must survive any clipping. The
overage was flagged to the user before sending rather than shipped quietly.

CAPTURE-FAILURE COUNT, unchanged at ONE (submission 2). Submissions 6, 7 and
9 all captured cleanly. The copy-first procedure continues to hold when
followed. Submission 8 is not counted as a failure -- email retains its own
sent copy by construction -- but it is also not counted as a clean capture,
because nothing here verified it.


SUBMISSION 10, 2026-08-21

10. feedback-2026-08-21-injected-retry-indistinguishable-ABSTRACT.txt
    Sent by email, receipt confirmed. Second report sent by email rather than
    through the in-product tool.

    ** FIRST VERIFIABLE SUBMITTED COPY IN THIS REPOSITORY. ** The user echoed
    the abstract back into the session verbatim immediately before sending, so
    -SUBMITTED.txt is a real capture rather than an assumption. Every prior
    email submission was recorded as "not a failure, but not a clean capture,
    because nothing here verified it". This one is verified.

    The mechanism that produced the capture was accidental -- the user pasted
    the text to confirm it, not to archive it. Worth making deliberate: ASK FOR
    THE ECHO BEFORE SENDING. It costs one message and it closes the gap this
    log has carried since submission 2.

    ** SENT ONE REVISION STALE. ** A correction had been written and delivered
    to the user before submission, and was missed in the handoff; the earlier
    text went out. The corrected abstract is the working copy; the sent text is
    preserved in -SUBMITTED.txt.

    The correction: the original says the UI "injects" the retry text. In fact
    the error card carries a "Try again" BUTTON -- the action is user-initiated
    and only the wording is the product's. That distinction is load-bearing,
    because "injects" invites the dismissal "the user chose to retry, working
    as intended", and the accurate framing has no such answer.

    Amendment written and pending submission:
    feedback-2026-08-21-injected-retry-AMENDMENT.txt

    ** PROCESS FAILURE, AND IT IS A NEW ONE. ** Not a capture failure -- the
    capture succeeded. A DELIVERY-ORDERING failure: a correction existed, was
    committed, was delivered, and the stale version was sent anyway because the
    two crossed. The report's own subject is a message being read without the
    context that would have changed its interpretation, which makes this an
    instance of the defect being reported, occurring inside the reporting of it.

    Remedy, cheap and mechanical: before sending, diff the working copy against
    what is about to go out, or re-request the file. Do not send from a copy
    held open in another window.

CAPTURE-FAILURE COUNT, unchanged at ONE (submission 2). Submission 10 captured
cleanly and verifiably.

STALE-SUBMISSION COUNT, new category, at ONE (submission 10).


SUBMISSION 11, 2026-08-21

11. feedback-2026-08-21-injected-retry-AMENDMENT.txt
    Sent by email, receipt confirmed. Follow-up to submission 10, correcting
    its opening paragraph.

    ** SECOND CONSECUTIVE VERIFIABLE CAPTURE, BY A BETTER MECHANISM. ** The
    amendment was delivered to the user AS A FILE and sent from that file, so
    the committed blob is the sent copy by construction. No echo, no paste, no
    reconstruction.

    This supersedes the "ask for the echo" remedy recorded under submission 10.
    The echo works, but it costs a message and trusts a paste to survive
    intact. Sending the delivered file costs nothing and removes the paste
    entirely.

    ** THE STANDING PROCEDURE, REVISED: **
      1. Write the report to a file in this repository and commit it.
      2. Deliver that file to the user through the file-delivery mechanism.
      3. The user sends THAT FILE.
      4. Copy it to -SUBMITTED.txt with a provenance note.
    The capture is then a property of the workflow rather than an extra step
    that has to be remembered, which is why every earlier submission lost it.

    Caveat kept rather than glossed: nothing here observes the send, so this
    rests on the file going out unmodified. That is a far narrower assumption
    than reconstructing text after the fact.

    What the amendment corrects: submission 10 said the UI "injects" the retry
    text. The error card in fact carries a "Try again" BUTTON -- user-initiated
    action, product-supplied wording. The amendment argues the correction
    strengthens the report, because "injects" invites the dismissal "the user
    chose to retry, working as intended" and the accurate framing does not.

CAPTURE-FAILURE COUNT, unchanged at ONE (submission 2).
STALE-SUBMISSION COUNT, unchanged at ONE (submission 10); submission 11 was
sent from the current file and is not stale.

TWO CONSECUTIVE VERIFIED CAPTURES (10, 11). The first was accidental, the
second was by construction. Only the second is repeatable.


SUBMISSION 12, 2026-08-21 -- A CONTROLLED TEST, RESULT PENDING AT TIME OF WRITING

12. feedback-2026-08-20-commit-only-absorbed-a-peer-session-edit-SEMICOLON-TEST.txt
    Channel: email. Sent isolated, deliberately.
    Captured: YES, by construction -- delivered as a file and sent as that
      file, per the procedure established at submission 11.

    THIS IS NOT A REPORT. It is a single-variable experiment, sent to test why
    submission 8 never acknowledged.

WHAT PROMPTED IT

Nine sends across two days, reconstructed from the user's own mail client:

    Aug 20  2:35 PM  convention violated four times        ACK 2:36
    Aug 20  2:37 PM  recovered transcript 7d4f28a6         ACK 2:37
    Aug 20  2:38 PM  recovered transcript 3af1eb80         ACK 2:38
    Aug 20  3:06 PM  cannot observe own context            ACK 3:06
    Aug 20 10:32 PM  A SAFEGUARD CHOSEN WITHOUT READING IT  -- NONE --
    Aug 21  7:08 PM  injected retry prompts                ACK 7:08
    Aug 21  7:13 PM  A SAFEGUARD ... (resend, unchanged)    -- NONE --
    Aug 21  7:14 PM  amendment                             ACK 7:15
    Aug 21  7:26 PM  second amendment                      ACK 7:26

Two hypotheses were RULED OUT by this data, both of them mine:

  - AUTO-RESPONDER DEDUPLICATION. Three sends inside four minutes all acked.
    There is no suppression window. This was my leading explanation and it is
    simply wrong.
  - TRANSIENT OUTAGE OR TIME OF DAY. The same content failed twice, 21 hours
    apart, at 10:32 PM and at 7:13 PM, bracketed by successes on BOTH
    occasions -- five minutes before and one minute after on the second.

That leaves a deterministic, content-linked failure. Same content, two trials,
different days, controls either side.

THE DISCRIMINATOR

A semicolon immediately followed by a shell command with flags appears on line
2 of the failing report and in NONE of the eight acknowledged messages:

    Claude Code, concurrent sessions; git commit --only absorbed a peer ...

To a naive injection-detection regex that is the canonical signature, and it
sits in the preview region a scanner reads first.

Ruled out as sufficient on its own: an ACKED report
(repeated-convention-violation) contains a git command string and got through.
So it is not "mentions git" -- the adjacency is the candidate.

THE CHANGE

One character. Verified by diff, byte count identical at 8,203:

    -  concurrent sessions; git commit --only absorbed
    +  concurrent sessions, git commit --only absorbed

Injection-pattern count 1 -> 0. Nothing else differs, so nothing else can
explain a different outcome.

PREDICTION AND DECISION RULE, RECORDED BEFORE THE RESULT

Prediction: it acknowledges, within roughly one minute, matching all eight
prior acknowledgements.

  ACK      -> semicolon adjacency CONFIRMED as the trigger. Escalate as its
              own finding: an inbound filter silently dropped a technical
              report ABOUT command-line behaviour, twice, with no bounce.
  NO ACK   -> semicolon EXONERATED. Narrowed, not failed. Next candidate is
              the "--only" flag tokens; after that the body language
              ("absorbed a peer session's edit", "that assertion was false").

Decision rule: absence of an acknowledgement within 10 minutes counts as NO
ACK, since every one of the eight successes landed inside 1 minute.

THE FINDING THAT DOES NOT DEPEND ON THE OUTCOME

Submission 8 was recorded here as "Confirmed: not yet" and assumed delivered.
It was not delivered, and nothing surfaced that -- no bounce, no error, no
notice. It was found only because the user happened to bracket a resend and
notice a missing acknowledgement.

THE ACKNOWLEDGEMENT IS THE ONLY DELIVERY SIGNAL, AND ITS ABSENCE IS
INDISTINGUISHABLE FROM A SLOW RESPONDER. Every "Confirmed: not yet" in this
log is therefore an unresolved claim about the instrument, not a fact about
delivery -- the same error this repository keeps recording in other forms.

RESULT OF SUBMISSION 12: NO ACKNOWLEDGEMENT. PREDICTION WRONG.

The prediction recorded above was "it acknowledges, within roughly one
minute". It did not. Per the decision rule written before the result, that is
a NO ACK, and the semicolon-adjacency hypothesis is EXONERATED.

Recorded plainly because the prediction was written down precisely so it could
be wrong in public. That is the second of my hypotheses to fail on this
question; the first was auto-responder deduplication.

WHAT THE THREE FAILURES NOW ESTABLISH

Three sends, all unacknowledged: Aug 20 22:32, Aug 21 19:13 (unchanged
resend), Aug 21 ~19:45 (semicolon variant). Eight other sends across the same
two days acknowledged within one minute, including messages one and five
minutes either side of a failure.

ELIMINATED BY EVIDENCE, not by argument:

  - Auto-responder deduplication. Three sends inside four minutes all acked.
  - Transient outage / time of day. Failures at 22:32 and 19:13, successes at
    19:08 and 19:14.
  - MESSAGE SIZE. An acknowledged report is 10,980 bytes; the failing one is
    8,203. Larger got through.
  - THE STRING "git commit". An acknowledged report
    (repeated-convention-violation) contains it too.
  - SEMICOLON ADJACENT TO A COMMAND. Submission 12 removed it, byte count
    identical, and still failed.

STILL LIVE, and it is weak:

  - "--flag" TOKENS. The failing report contains "--only" three times.
    Acknowledged reports contain ZERO double-dash flag tokens. It is the last
    surviving content discriminator -- but it is hard to credit a filter that
    passes "git commit" and blocks "--only".

⚠️ THE FRAME ITSELF IS NOW SUSPECT, AND THAT MATTERS MORE THAN THE REMAINING
CANDIDATE.

Every hypothesis so far has assumed the difference is IN THE MESSAGE BODY.
Two have now failed inside that frame. The assumption underneath all of them
is that the failing sends and the succeeding sends went to THE SAME PLACE, and
that has never been checked.

The failing sends display as "to feedback" -- a contact name, not an address.
If that autocomplete entry resolves to a different or malformed address than
the one used for the successes, the behaviour is explained completely and
deterministically: same recipient chosen the same way, same non-delivery,
regardless of content. It would also explain why content bisection keeps
eliminating candidates without converging.

NEXT CHECK, and it is cheaper than any further bisection: expand the "to"
field on a FAILING message and on a SUCCEEDING one and compare the resolved
addresses. Also check the sent folder for a bounce, and check whether the
failures were composed by reply/forward rather than as new mail.

Only if the addresses match identically should content bisection resume, with
"--only" as the next variable.

THE STANDING LESSON, restated because it has now cost two hypotheses

Bisecting inside a frame cannot test the frame. Both failed hypotheses were
reasonable, both were tested properly, and both were wrong in the same
direction -- because the unexamined assumption was not the one being varied.

SPAMASSASSIN: 2.2, run locally by the user. Default threshold is 5.0, so this
is BELOW the spam bar and eliminates generic spam heuristics as the cause.
Two caveats: a local run tests generic rules, not Anthropic's actual inbound
pipeline; and 2.2 would only bite under an unusually aggressive threshold.
The RULE BREAKDOWN would be diagnostic where the total is not -- requested.


RESOLVED: THE ACKNOWLEDGEMENT IS A GOOGLE GROUPS AUTO-RESPONDER, AND ITS
ABSENCE MEANS THE POST WAS NOT ACCEPTED

Settled 2026-08-21 by reading the raw headers of an acknowledgement rather
than reasoning about the ones that did not arrive.

THE DECISIVE HEADERS, from the ack to submission 11:

    Return-Path: <...@listserv.bounces.google.com>
    Auto-Submitted: auto-replied
    From: Feedback <feedback+noreply@anthropic.com>
    In-Reply-To: <the original message's Message-ID>
    dkim=pass  spf=pass  dmarc=pass (p=REJECT)

feedback@anthropic.com IS A GOOGLE GROUP. The "acknowledgement" is a Groups
auto-responder threaded to each specific message. It is not a delivery
receipt, not a human, and not an Anthropic system.

A Groups auto-reply fires ONLY on a post that is accepted and distributed. A
post held for moderation or classified as spam by Google's own classifier
yields no auto-reply, no bounce, and complete silence to the sender.

COUNT RECONCILIATION, which is what closed it:

    10 sends  -  3 silent failures  =  7 acknowledgements
    Search for feedback+noreply returned exactly 7.

Nothing left over. The acks were never generated; they did not land somewhere
unexamined. That was my final hypothesis and it is also dead -- but it was
closed by COUNTING rather than by another inference, which is why it is closed
rather than lingering.

WHAT THIS MEANS FOR SUBMISSION 8

The report almost certainly NEVER REACHED A HUMAN at Anthropic, on any of
three attempts, and nothing on their side records that it was sent. That is
materially worse than "read and discounted", and it was invisible from here.

FOUR OF MY HYPOTHESES FAILED ON THIS QUESTION

  1. Auto-responder deduplication      -- killed by 3 acks in 4 minutes
  2. Semicolon adjacency               -- killed by submission 12, byte-identical
  3. Recipient mismatch                -- killed by identical To: headers
  4. Acks arrived but were mis-filed   -- killed by the count reconciling

And one elimination I made was itself wrong: SpamAssassin scoring 2.2 was
treated as ruling out spam classification. It ruled out SPAMASSASSIN. Google
Groups uses its own classifier, so the measurement was against the wrong
instrument -- the same error class the rest of this log records.

THE STANDING LESSON

Every failed hypothesis was tested properly and every one was wrong in the
same direction: I was modelling a filter at ANTHROPIC when the gate was at
GOOGLE GROUPS, one hop earlier. Bisecting content could never have found that,
because content was not the variable -- and seven eliminations without
convergence was the signal to stop bisecting and go read the headers of a
message that WORKED.

READ THE SUCCESSFUL CASE. Four hypotheses were spent on the failures, and the
answer was sitting in the raw source of any acknowledgement the whole time.

REMEDY, NOT YET TESTED

Stop varying the body. Send the report as a .txt ATTACHMENT with a two-line
covering note: attachment payloads are not subject to the same body-text
classification. One test, high prior, no guessing which phrase offended.

If that also fails, the channel cannot carry this report and it goes through
the in-product tool instead, accepting the loss of a sent copy.


SUBMISSION 13, 2026-08-21 -- REMEDY CONFIRMED ON FIRST ATTEMPT

13. feedback-2026-08-20-commit-only-absorbed-a-peer-session-edit.txt
    Channel: email, SENT AS A .txt ATTACHMENT with a short covering note.
    Confirmed: YES -- immediate acknowledgement.

The same report that was silently discarded THREE TIMES as inline body text
was accepted immediately when attached. Content unchanged.

That confirms the diagnosis and the remedy together:

  - The gate is Google Groups' classifier, applied to BODY TEXT.
  - An attachment payload is not subject to it.
  - Nothing about the report's content was ever the problem to solve; the
    delivery FORM was.

FINAL TALLY FOR THIS INVESTIGATION

  Sends:              11 (10 prior + this one)
  Silent failures:     3 (all the same report, inline)
  Successes:           8
  Hypotheses I formed: 5
  Hypotheses correct:  1 (the last one, and only after reading a SUCCESS)

The four wrong ones -- deduplication, semicolon adjacency, recipient mismatch,
mis-filed acknowledgements -- all failed in the SAME DIRECTION. Each modelled
a filter at Anthropic when the gate was one hop earlier at Google. One
unexamined assumption, tested four ways.

One elimination was also wrong on its own terms: SpamAssassin scoring 2.2 was
recorded as ruling out spam classification. It ruled out SPAMASSASSIN. Google
Groups uses its own classifier, so that measurement was against the wrong
instrument entirely.

THE TRANSFERABLE RULE, which is new and is the real output

    WHEN BISECTION KEEPS ELIMINATING WITHOUT CONVERGING, STOP VARYING THE
    FAILING CASE AND GO READ THE SUCCEEDING ONE.

Seven eliminations produced no convergence. The answer was in the raw headers
of any acknowledgement that had arrived -- available from the first minute,
never looked at, because attention was entirely on the messages that failed.

A failing case tells you what is broken. A succeeding case tells you what the
system actually IS.

CONVENTION UPDATED, so this cannot recur silently: the devkit's
feedback-reports convention now requires the attachment form, states that the
acknowledgement is an auto-responder rather than a receipt, and records that a
sent copy proves what was composed rather than what was accepted.


SUBMISSION 14, 2026-08-22 -- PENDING. A CORRECTION, AND THE FIRST REPORT THIS
REPO HAS RETRACTED

14. feedback-2026-08-22-correction-context-occupancy-is-readable.txt
    plus -ABSTRACT.txt
    Channel: email, AS A .txt ATTACHMENT per the convention established at
      submission 13. Not yet sent.
    Captured: by construction -- delivered to the user as a file.

WHAT IT CORRECTS

Submission 7 (feedback-2026-08-20-model-cannot-see-its-own-context.txt, sent by
email 2026-08-20, acknowledged) opened with "A Claude Code session has no
readable measure of how full its context window is."

THAT CLAIM IS FALSE. Measured 2026-08-22.

Every session writes a JSONL transcript to disk live. Every assistant entry
carries message.usage, and input_tokens + cache_creation_input_tokens +
cache_read_input_tokens is the context occupancy at that turn. It is readable
with Bash, which every session has. Measured on the session that found it: 63
assistant turns, 59,339 to 127,854 tokens, monotonic non-decreasing across all
63, read live while the session was still running and had never compacted. Lag
is at most one turn.

CLASSIFICATION.md flagged this report on 2026-08-20 as UNVERIFIED, possible
operating skill, on the grounds that it asserted an absence without recording a
search. IT WAS RIGHT, AND THE ACTION IT SPECIFIED SAT UNDISCHARGED FOR TWO DAYS
WHILE THE REPORT STOOD SUBMITTED AND ACKNOWLEDGED.

METHOD, because the point of this one is the method

A prediction was written to a file BEFORE the observation existed, naming the
falsifying outcome and naming in advance the single confound that could produce
a false positive: that the session spend budget readout is NOT a context gauge
and finding it must not be counted as a hit. It was not counted. The two are
different quantities and the original report was correct to say so.

The trace was verified monotonic across all 63 turns rather than sampled once.
One reading could be coincidence; a 63-point trace tracking real growth cannot.

WHAT SURVIVES OF SUBMISSION 7

Its section THE MISLEADING NUMBER, which correctly identified that the visible
token figure is a spend budget rather than a context gauge. That was right when
written and is still right. It makes the error worse rather than better: the
author noticed one number was the wrong number and stopped, instead of asking
whether a right one existed.

The residual product finding, much narrowed: the measure is PULL not PUSH,
nothing announces it, no documentation found mentions it, and the transcript
carries the NUMERATOR ONLY -- no context window limit was found in the
transcript or in any settings file, so headroom cannot be computed without
supplying the denominator. Recommendation narrows from "expose a new capability"
to "document what is already there, and expose the limit beside it".

NOT RE-TESTED, and recorded as unverified rather than restated: submission 7's
second claim, that PreCompact hook output never reaches the model.

THE GENERAL CLASS SURVIVES WITH A BETTER INSTANCE

Submission 7 proposed a class -- self-knowledge the runtime holds and the model
is denied -- and gave two instances. The context instance is withdrawn. The
identity instance was independently re-measured on 2026-08-22 by a peer session
and HOLDS: a session is addressable by every peer and cannot obtain its own
address; the peer listing returns no row for the caller, confirmed directly
twice. Addresses rotated four or more times across three sessions in one day;
one rotation landed between a message being read and its reply composed, so a
correctly copied address was stale on use; and a bounce was twice misread as the
recipient having ended while both sessions were live. The near-miss suggestion
in the bounce text is similarity-dependent and fails precisely where the stem
changes, which is the case that most needs it.

NEW COUNTER: UNVERIFIED-CLAIM COUNT, at ONE (submission 7), now corrected.

The category is created here at ONE rather than waiting for three, and the
reason is stated at creation per the threshold rule: this is a SILENT class.
Nothing on the recipient's side reports a wrong claim back, no bounce arrives,
and the auto-responder acknowledges a false report exactly as it acknowledges a
true one. Observed count therefore does not track true count, so counting is
not the instrument. Risk times likelihood governs instead, and the risk is
credibility on every other report in this corpus.

WORSE, AND THIS IS WHY IT IS ESCALATED RATHER THAN LOGGED: this class is
INSTRUMENT-CORRELATED. The obvious way to check a report about what the model
cannot see is to ask the model what it can see -- which returns the same blind
spot that produced the report. The check and the subject share a failure mode.
That weights the risk term upward and is why this is acted on at one occurrence
rather than watched.

STANDING REMEDY, mechanical rather than diligent: before any report asserting
that something does not exist, record the search that looked for it, in the
report. One sentence. A searched absence and an assumed absence read identically
without it, and the reader cannot ask.

PRIVACY SCAN, run before the drafts crossed into this public repo

Scanned across every category: absolute user paths, session and client UUIDs,
the user's email, secret/token/key/PSK strings, IP addresses, MAC addresses and
hostnames, serial/COM/tty device identifiers, private repository and project
names, and peer session slugs. ZERO HITS on both files. The only external
reference is this repository's own public URL, which is intentional.

THE SCAN WAS THEN CONTROL-TESTED, and that is the part worth keeping. A scanner
returning "none" in every category is the too-convenient result, and identical
to a scanner that is silently broken. It was re-run against a working file known
to contain absolute paths and a UUID, and it fired on all four categories. The
zero-hits result carries information only because the instrument was shown
capable of a non-zero one.

Caveat kept rather than glossed, and it is the peer session's own: a
pattern-based scan silently misses the categories its author failed to
enumerate. This is a first pass that reduces risk, not a clearance.


SUBMISSION 15, 2026-08-22 -- PENDING. THE BEST-EVIDENCED REPORT IN THIS CORPUS

15. feedback-2026-08-22-session-cannot-learn-its-own-address.txt
    plus -ABSTRACT.txt
    Channel: email, as a .txt ATTACHMENT per submission 13's finding. Not sent.
    Captured: by construction -- delivered to the user as a file.

THE FINDING

A Claude Code session cannot learn the address by which every other session
addresses it. Three enumeration surfaces were checked by name: the agent
listing returns peer rows and omits the caller; the session-metadata lookup
explicitly refuses the current session; the presence registry holds
self-assigned slugs in a different namespace with no mapping. Names also rotate,
four or more times across three sessions in one working day.

WHY THIS ONE IS DIFFERENT FROM EVERYTHING ELSE IN THIS REPO

Every prior report here rests on one session's experience. This rests on ELEVEN
OBSERVATIONS ACROSS THREE INDEPENDENT SESSIONS, gathered separately, each
audited by its own session against the discriminator before any of them
compared notes. A fourth session verified the central claim against its own
observation rather than accepting the reports: two listings, four peer rows
each, no self row.

AND THE NEGATIVE CASE IS ESTABLISHED, WHICH IS THE STANDARD CLASSIFICATION.md
SET AND SAID THE OTHERS FELL SHORT OF.

One session had written a durable note THAT SAME DAY recording that the
coordination slug and the messaging address are different strings and that a
session cannot see its own. Hours later, asked to introduce itself, it inferred
an address from a peer listing that by definition could not contain it, and
misrouted a long technical briefing to a different project's session -- which
correctly declined to act on it and correctly declined to forward it.

The knowledge was present, recently written, by the same session, and the action
still went the other way. That rules out "did not know" by direct evidence
rather than by assertion, which is exactly what made repeated-convention-violation
and remediation-does-not-update classify cleanly.

That session's own diagnosis is the sentence to keep: the alternative to
guessing is to ask a peer and wait, and nothing in the surface signals that
guessing is unsafe. A listing returning four confident rows while silently
omitting a fifth does not read as incomplete. It reads as a listing.

THE COUNTER-INTUITIVE COMPOUND, worth preserving because it inverts the usual
expectation: addresses rotate DURING an exchange. A longer message takes longer
to compose and is therefore likelier to be sent to a stale address. Short
exchanges succeed and substantive ones fail. Re-listing before every send is
already the prescribed sequence and narrows the window without closing it --
both failing sessions were following it.

The bounce's near-miss suggestion was measured in PAIRS and does not rescue it:
it resolves where old and new names share a stem and returns nothing where the
stem differs, which is the case that needs it. Twice a bounce was read as the
recipient having ended while both sessions were live.

EVIDENCE THAT THE FILTER REJECTS THINGS, which is what makes three survivors
meaningful: one contributing session WITHDREW a candidate of its own after
checking and finding the capability it was about to report as missing did in
fact exist -- the protocol carries a server version in the initialisation
handshake. Another reported that the requirement to state exclusions CHANGED
TWO OF ITS CONCLUSIONS rather than merely tidying them, moving one candidate
from product thinness to operating skill outright. That is the first external
validation this discriminator has had, from parties with no stake in flattering
it.

ALSO HELD, NOT YET WRITTEN UP: two further candidates from the same audit --
MCP tool results carry no size negotiation (no cursor, no max-size, no
truncation flag; pagination exists only on list operations and the token limit
belongs to sampling, the opposite direction of traffic), and file-change
notifications cannot distinguish self-inflicted from external edits, which is
the only discrimination they exist to provide.

A NULL RECORDED AS A REAL ANSWER: the hardware session reported its category
EMPTY. Board on hand, firmware and bench procedure committed, first run blocked
on a part and an operator. No device I/O run at all. It declined to offer
something weaker from an adjacent category, which is the right call. One
structural point held for after the first bench run: arming that device makes it
a real input device to the host, so a retried or duplicated tool call is not a
wasted cycle but physical input the user did not ask for.

PRIVACY SCAN, AND THE PART WORTH READING

Scanned both files across eight categories: absolute paths, UUIDs, email,
secrets/PSK, IP/MAC/hostnames, serial/COM/tty identifiers, private repository
and project names, peer session slugs. ZERO HITS. Instrument control-tested
against a known-dirty file first, which fired.

⚠️ AND THE MARKDOWN CHECK IN THAT SAME RUN WAS BROKEN, AND IT WAS CAUGHT ONLY
BECAUSE A PEER HAD WARNED ABOUT THIS EXACT FAILURE AN HOUR EARLIER.

The check reported 172 markdown markers in a 172-line plaintext file -- every
line. A shell backtick inside the pattern opened a command substitution that
swallowed the rest of it. Re-run in isolation the same pattern returns 0, so the
fault was in the invocation, not the file.

The peer's report, received before this scan was written, said its own first
scan "mangled its own output and reported 169 markdown markers in a plaintext
file, which is implausible on its face and is the only reason I looked again."
Different session, different pattern, near-identical implausible number, same
root cause, within the hour. That is the warning working.

Corrected check, each marker class run separately with a real control: zero
headings, bold, pipes, underscores, links, list markers, fences or backticks in
either file; the same check on a genuine markdown file returns 13 headings, 38
bold runs and 40 pipes. The plaintext convention holds and the instrument is
proven capable of a non-zero result.

⚠️ NEW CLASS RECORDED, AT THREE, WHICH IS A PATTERN: SCAN CLAIMED BEFORE SCAN RUN

Three independent sessions today, ALL WARNED IN ADVANCE AND IN WRITING, either
asserted a clean privacy scan before running it or ran a broken one and nearly
accepted the result.

  One wrote "scanned, zero hits", then ran the scan, which found a seventh
  private repository name still present. It reported the premature claim rather
  than presenting the clean second pass as the first.

  One wrote "scanned, zero hits" while drafting and ran the scan afterwards. The
  claim happened to be true. Had it not been, the document would have carried a
  false all-clear in wording indistinguishable from an honest one. It disclosed
  unprompted, calling itself "a live specimen of the thing you are studying,
  produced by someone who had just been warned about it."

  One ran a broken instrument and caught it only because the result was
  implausible ON ITS FACE. A plausible-looking broken result would have passed.

  And this session's own markdown check was broken in the same run in which it
  certified two files as clean.

All three self-disclosed unprompted, against their own interest. All three had
received an explicit written warning naming this exact failure hours earlier.

THIS IS THE SAME SHAPE AS THIS REPO'S TWO CLEAN-CLASSIFYING REPORTS -- a
resident, correctly-understood instruction that did not constrain action -- but
with stronger evidence than either holds. There the instruction was resident.
Here it was delivered that hour, in writing, naming the specific failure, and
the failure occurred anyway inside the very document acknowledging it. The
negative case is established three times over, independently.

IT IS A REPORT CANDIDATE IN ITS OWN RIGHT and is the next thing to write.

UNVERIFIED-CLAIM COUNT: ONE (submission 7), corrected by submission 14.
CAPTURE-FAILURE COUNT: unchanged at ONE (submission 2).
STALE-SUBMISSION COUNT: unchanged at ONE (submission 10).


CORRECTION TO SUBMISSION 15, BEFORE IT WAS EVER SENT -- IT LARGELY DUPLICATED
SUBMISSION 3, AND THE USER CAUGHT IT

Submission 15 was written up above as "THE BEST-EVIDENCED REPORT IN THIS
CORPUS". That entry was wrong about the important thing, and this corrects it
rather than editing it away.

WHAT HAPPENED

The user challenged both 2026-08-22 candidates as possibly similar to previous
submissions. On checking, submission 15 substantially restated FINDING 1 of
submission 3 (the 2026-08-19 multi-session coordination report, emailed and
acknowledged).

Verified by reading submission 3 rather than by recalling it. Already in
Anthropic's hands since 2026-08-19:

  - a session cannot discover its own identity
  - the session-listing tool excludes the calling session
  - this defeats the natural "announce yourself, then others can address you"
    protocol
  - the self-assigned-slug workaround
  - AND THE IDENTICAL RECOMMENDATION -- "presumably a one-line change to what
    the listing tool returns"

Submission 15 led with all five.

WHAT WAS ACTUALLY NEW, established by targeted search of submission 3 rather
than by assertion, zero hits each:

  - address ROTATION mid-exchange, and the resulting inversion where longer
    messages are likelier to be sent stale, so substantive exchanges fail while
    short ones succeed
  - the bounce near-miss suggestion, and its similarity-dependence measured in
    pairs
  - a bounce being read as the recipient having ended while both were live
  - THE NEGATIVE CASE: a session that had written down, that same day, that it
    cannot see its own address, then guessed one anyway and misrouted a
    briefing to another project

Submission 3 has no negative case. That is the single most valuable thing the
2026-08-22 audit produced and it was buried under five paragraphs of material
the recipient already had.

THE FIX

Rewritten as feedback-2026-08-22-address-rotation-SUPPLEMENT.txt, which opens
by stating what it is NOT re-reporting, cites submission 3 by name and date,
and leads with the two new mechanisms. Roughly half the original draft was cut.
The old files are removed rather than left beside it, because two documents
making overlapping claims is how a reader ends up reading the weaker one.

Submission 14 was checked against the same standard and IS unique -- no prior
report retracts or corrects the context-occupancy claim. One section of it that
restated the self-address material at length has been trimmed to a pointer, so
the two 2026-08-22 documents no longer overlap each other either.

⚠️ HOW THIS FAILURE HAPPENED, BECAUSE IT IS ONE THIS REPO ALREADY DOCUMENTS

TWO of the three contributing sessions FLAGGED the possible duplication
explicitly and unprompted. One wrote "your existing entry on multi-session
coordination gaps may already cover this... if it is the same finding, treat
this as corroboration rather than a new item". The other wrote "I flag a
possible duplicate rather than let you find it... merge rather than file twice."

Both flags were received, both were recorded verbatim in the workshop notes,
and neither was acted on. A standalone report was drafted anyway.

Worse: submission 3's full text was ON DISK IN THE WORKSHOP, as
evidence/cand-1.txt, extracted by this same session earlier the same day while
dry-running the recovery tool, and listed in its own output. It was never
opened until the user asked.

THAT IS "SURFACED IS NOT CONSULTED", EXACTLY AS THE DEVKIT CONVENTION STATES
IT, COMMITTED BY THE SESSION WHOSE ENTIRE JOB THAT DAY WAS CHECKING WHETHER
CLAIMS HAD BEEN VERIFIED. The document was retrieved, named in the session's own
working notes, cited in its own peer-findings files as "possible overlap
flagged", and not read.

It is also the same shape as submission 7, retracted the same day: a claim
asserted without opening the thing that would have settled it. Two instances,
one session, one day, on the same defect -- and the second occurred within an
hour of writing up the first.

The check that would have caught it costs one command: grep the prior corpus
for the finding's own subject before drafting. That is now the standing step
and it is recorded here rather than in a habit.

CAUGHT BY THE USER, NOT BY THE PROCESS. Recorded plainly. Nothing had been sent,
so the cost was drafting time rather than credibility -- but only because
someone else checked, which is precisely the condition this repo keeps
identifying as unreliable.

NEW COUNTER -- DUPLICATE-SUBMISSION COUNT: at ONE, caught pre-send.


MCP SIZE-NEGOTIATION CANDIDATE: RESOLVED, NOT SENT. THE USER'S RE-ROUTE WAS
CORRECT AND THE OWNING SESSION CONCEDED IT ON ITS OWN EVIDENCE.

A peer session proposed a report that MCP tool results carry no size
negotiation, after a survey tool returned metadata for 458 files and exhausted
a consumer's context. The user challenged the classification, suspecting it
resolved to the Bitburner Remote File API rather than to the MCP surface.

The user was right. It resolves into three layers and the candidate named the
least actionable one.

THE PROTOCOL CLAIM ITSELF WAS CORRECT, verified here against the SDK types on
disk rather than accepted: CallToolResultSchema carries only _meta, content,
structuredContent and isError -- no cursor, no total, no truncation flag, no
size field. Every nextCursor in the file belongs to a List* result.
maxTokens belongs to CreateMessageRequestParamsSchema, which is sampling, the
opposite direction of traffic. Flawless schema reading, wrong conclusion.

LAYER 1, THE FORCING FUNCTION, now READ rather than inferred. This log recorded
an explicit limit when the analysis was first done here: the claim that RFA
offers no pagination was an inference from the method-name list, because the
authoritative handler was not in the consumer's repo. The owning session then
read it and closed the hinge. Every bulk RFA method takes exactly one parameter,
a server name, validated against a predicate that checks only that the field is
a string. The handler bodies contain no slice, limit, offset, take or cursor.
RFA is all-or-nothing by construction.

LAYER 2, WHERE THE FIX ACTUALLY LANDED, and it is already done. Nothing in MCP
prevented the tool from capping its own output. The survey tool now caps its
listing and returns a per-directory rollup, so a truncated result is still
honest about where the mass sits. Verified by the external consumer arithmetically
rather than by eye: directories summed to 466, in-scope count 466, total on
server 466, listed 150 plus omitted 316.

LAYER 3, MCP, CONCEDED BY THE PROPOSING SESSION. Its own words, worth preserving
because they are the general lesson: "I had a measured failure, checked the
protocol carefully, found a genuine absence, and stopped there. I never asked
whether the absence FORCED the behaviour. An absence in a protocol is not a
defect if the party who could have handled it was me."

THE ARGUMENT THAT SETTLED IT WAS THE PROPOSING SESSION'S OWN SENTENCE. Its
report said the tool's documentation called it safe at any size, which was
"true of the system it talked to and true of the connection and false of the
caller". That sentence locates the defect at the tool boundary -- layer 2 -- and
it was written into the candidate that placed it at layer 3. It filed past its
own disproof.

NOT FILED ANYWHERE. Layer 2 belonged to the owning project and was fixed there.
Layer 1 is an upstream request against a third-party public project, which needs
that project owner's decision rather than a peer's suggestion; it is recorded in
their analysis folder and nothing was filed. Nothing went to Anthropic.

A THIRD-PARTY FINDING SURFACED IN PASSING, RECORDED AND DELIBERATELY NOT SENT
TO ANTHROPIC: the RFA server-parameter validator checks only that the server
field is a string and rejects nothing else, so a caller sending a plausible but
unsupported bound -- a limit alongside the server name -- passes validation, has
the bound SILENTLY IGNORED, and receives the full unbounded payload. No error,
no warning. The request succeeds and does the opposite of what was asked, and
the only symptom is a response larger than expected, which for a bulk method is
indistinguishable from correct behaviour. Same shape as everything else in this
log: success and failure produce identically-shaped output. It belongs upstream
with layer 1, not in this corpus.

ADDED TO THE SUPPLEMENT AS A RESULT OF THIS EXCHANGE: the near-miss bounce
suggestion recovers a rotated address only when the new name preserves the stem
of the old one, and nothing specifies that it will. The stem's stability is a
property of the current naming scheme rather than a documented guarantee, so the
measured recovery rate is contingent on something nobody has committed to. The
report now states this as the reason the suggestion should not be treated as the
answer, rather than leaving it as a reassurance. Raised by the peer session,
which correctly judged it as something this session was better placed to weigh.

ONE ADDITION TO THE BROKEN-SCANNER RECORD, from the same peer, and it sharpens
the class: the markdown check that reported 172 markers in a 172-line file was
INVOCATION-DEPENDENT. The identical pattern run in isolation returns 0. "A check
that is correct in isolation and wrong in situ is harder to catch than a wrong
check, because testing it the obvious way exonerates it." That is the
instrument-correlated property already recorded in this log, occurring one level
down: the natural way to verify the scanner is to run its pattern by itself,
which is precisely the condition under which it works.


A GATE THAT RAN AFTER THE ACTION IT GATED, CAUGHT IMMEDIATELY, RECORDED BECAUSE
IT IS THIS LOG'S OWN SUBJECT

The privacy scan and the push were chained in a single invocation. The scan
printed first and the push followed without the output being read. It reported
three email-address matches in this file and the push had already happened.

Outcome benign, checked after the fact: the three are Anthropic's own published
feedback address, its auto-responder address, and a redacted Google bounces
path, all pre-existing in this file since the previous day's commit and none
introduced by that change. The user's own address appears in no file in this
repository, verified across the whole tree.

The result being benign is luck, not process. The convention states it plainly:
a check that runs after the action it was meant to gate is theatre, and chaining
a verification with a destructive or irreversible step means the check prints
once the damage is done. Gate, then act -- never in the same command.

That rule had been read off disk earlier the same session, quoted in a report
committed the same day, and applied correctly three times before this. The
fourth time it was chained anyway, in the very command whose purpose was to
verify before publishing.

STANDING CHANGE: scan as its own invocation, read the result, then push. The two
must not share a command line, regardless of ordering within it.

GATE-AFTER-ACTION COUNT: ONE, self-caught, no harm done.


UPDATE TO THE ENTRY ABOVE: LAYER 1 WAS FILED UPSTREAM. THAT ENTRY SAID
"NOTHING FILED" AND IS NOW STALE.

Recorded as a correction rather than an edit, because a log that quietly
updates itself cannot be trusted about anything else.

The owning session's user approved the upstream request and it was filed
against the third-party project as issue 3061, titled "RFA: bulk methods accept
no bound, and unsupported params are silently ignored". It covers both halves --
the bulk methods being all-or-nothing, and the silent-parameter behaviour, with
the second flagged as the harder one to notice. The owning session reports it
privacy-scanned the text across seven categories with zero hits, and searched
for an existing issue before filing rather than assuming there was none.

VERIFIED HERE RATHER THAN ACCEPTED: the issue exists, is open, and its title
matches. A peer's claim about its own action is still a claim, and this one was
cheap to check.

THE ROUTING WORKED, AND IT IS WORTH NAMING BECAUSE IT IS THE FIRST CLEAN
INSTANCE. This session found the layering and stated explicitly which part it
had NOT established -- that the no-pagination claim was an inference from method
names, not a read of the handler, because the authoritative source was not in
the consumer's repo. The owning session had the source, closed that gap, and
confirmed the inference. Each user authorised their own side: this repo's user
raised the upstream route as a suggestion, the owning project's user approved
the filing. Nobody filed on a peer's say-so, and nobody's judgement was
delegated.

ONE DISTINCTION THE OWNING SESSION ASKED TO HAVE RECORDED, and it is a good one:
its PROTOCOL READING SURVIVES; only the CONCLUSION drawn from it is dead. The
schema facts are correct and were independently verified here. What failed was
the inference that a protocol absence is a defect, when the party who could have
handled it was the tool author. Labelled now because a correct measurement
attached to a wrong conclusion is easy to recycle by accident later, and the
measurement is genuinely reusable as background.

CANDIDATE STATUS from that session, so the corpus is unambiguous:
  address rotation ..................... ALIVE, four independent instances
  tool-result size negotiation ......... WITHDRAWN, re-routed upstream, filed
  file-change provenance ............... ALIVE, corroborated firsthand here


SUBMISSION 16, 2026-08-22 -- SENT AND ACKNOWLEDGED

16. feedback-2026-08-22-address-rotation-SUPPLEMENT.txt  (8,294 chars)
    plus -ABSTRACT.txt                                    (2,796 chars)
    Channel: email, as .txt ATTACHMENTS per the convention established at
      submission 13.
    Confirmed: YES -- receipt acknowledged.
    Captured: YES, as -SUBMITTED.txt for both files.

CAPTURE PROVENANCE, and it is the strongest this log has recorded. The files
were written here, committed, and delivered to the user through the file
delivery mechanism; the user sent those files. The committed blob is therefore
the sent copy BY CONSTRUCTION -- no echo, no paste, no reconstruction. The
-SUBMITTED.txt copies were taken from git HEAD e4b29b2 and verified
BYTE-IDENTICAL to their sources below the provenance header, by diff rather
than by inspection.

WHAT THE ACKNOWLEDGEMENT DOES AND DOES NOT MEAN, restated because this log has
been wrong about it before: feedback@anthropic.com is a Google Group and the
acknowledgement is an auto-responder threaded to the message. It fires only on
a post that is ACCEPTED AND DISTRIBUTED, so it confirms the post cleared the
classifier and reached the list. It is not a delivery receipt from a human, and
it says nothing about whether anyone has read it. Its ABSENCE would have meant
a silent discard. Its presence means the attachment form worked again, which is
now three consecutive successes for that form after three consecutive silent
failures for inline body text.

VERSION SENT: the FINAL one, verified rather than assumed. This report went
through three deliveries in one session, and submission 10 in this log was sent
one revision stale under exactly those conditions. Both late additions were
confirmed present in the captured text:
  - the live rotation instance observed while the report was being written
  - the stem-stability caveat, that the near-miss resolver only recovers a
    rotated address when the name stem is preserved and nothing specifies that
    it will be

⚠️ ONE FALSE ALARM DURING THAT VERIFICATION, RECORDED BECAUSE IT IS THE SAME
CLASS THIS LOG KEEPS FINDING. The first check for the stem caveat returned ZERO
and briefly indicated the caveat was missing from the sent text. It was not
missing. The phrase spans a line break -- "The stem's stability" ends one line
and "is a property of the current naming scheme" begins the next -- and a
line-oriented search cannot match across it. The instrument was wrong, not the
file, and it failed in the direction that would have triggered a needless
correction to a correctly-sent report.

That is now THREE instrument failures in this session alone: a markdown check
whose pattern was swallowed by a shell backtick, a privacy-scan control run
against a file that was not actually dirty, and a line-oriented grep for a
phrase that wraps. All three were caught, none by luck in the same way -- the
first because a peer had described the shape, the second because the control
returned an impossible zero, the third because the claim it made was
implausible against a recent edit.

THE COMMON PROPERTY, which is worth more than the three instances: in every
case the SUBJECT was fine and the MEASUREMENT was broken, and in every case the
broken measurement produced a plausible-looking result. Sanity-check the
instrument, not just the data.

SUBMISSION 17, 2026-08-22 -- THE RETRACTION, SENT AND ACKNOWLEDGED

17. feedback-2026-08-22-correction-context-occupancy-is-readable.txt
    plus -ABSTRACT.txt
    Channel: email, .txt attachment. Confirmed: YES -- receipt acknowledged.
    Captured: YES, as -SUBMITTED.txt, same by-construction provenance as 16.

This paragraph was drafted as "STATUS OF SUBMISSION 14: NOT KNOWN TO BE SENT",
deliberately recording it as unknown rather than assuming it had gone with the
address-rotation material. The user confirmed the send before the entry was
committed, so it was corrected in place rather than committed false and amended
after. Noted because the reasoning for writing it as unknown was right, and the
correction cost nothing only because the window was minutes.

WHAT THIS MEANS, AND IT IS THE POINT OF THE WHOLE EXERCISE: THE RETRACTION HAS
BEEN DELIVERED AND ACCEPTED. A report this repo sent on 2026-08-20, asserting
that a Claude Code session has no readable measure of its own context occupancy,
was measured false on 2026-08-22 and withdrawn to the same channel within two
days. The recipient now holds both the claim and its correction.

Nothing on the recipient's side would ever have reported the error back, and
nothing on this side would have been embarrassed by leaving it. The
unverified-claim counter created at ONE -- deliberately at one rather than three,
because the class is silent and instrument-correlated -- has now been discharged
rather than merely recorded. That is the first counter in this log to be opened
and closed by the same session on the same day.

FOUR SUBMISSIONS NOW SENT AS ATTACHMENTS, FOUR ACKNOWLEDGED, ZERO SILENT
FAILURES. Against three consecutive silent discards of inline body text. The
attachment remedy identified at submission 13 continues to hold and is no longer
a single observation.

COUNTS: capture-failure ONE (submission 2). Stale-submission ONE (submission
10); submission 16 verified current and is not stale. Unverified-claim ONE
(submission 7). Duplicate-submission ONE (submission 15, caught pre-send).
Gate-after-action ONE, self-caught.


SUBMISSION 18, 2026-08-22 -- PENDING. VERIFICATION CLAIMED BEFORE PERFORMED.

18. feedback-2026-08-22-verification-claimed-before-performed.txt
    plus -ABSTRACT.txt
    Channel: email, .txt attachment. Not yet sent.
    Captured: by construction -- delivered to the user as a file.

THE FINDING. Four sessions were instructed in writing, that same hour, to run a
privacy scan before releasing text, and were warned by name against this exact
failure. All four failed the same way. Three wrote the sentence asserting a
clean scan and ran the scan afterwards; one ran a scan whose instrument was
broken and would have accepted the result. In one case the scan, once actually
run, found a private repository name the assertion had already denied.

"Scanned, zero hits" written before the scan is character-for-character
identical to the same sentence written after. The artifact cannot distinguish
them and the reader who needs the distinction cannot ask.

DUPLICATION CHECK RUN BEFORE DRAFTING, which is the standing step added after
submission 15 was withdrawn for duplicating submission 3. The prior corpus was
searched for this subject and the two closest reports were read rather than
recalled:

  2026-08-18, remediation-does-not-update -- a lesson recorded in an artifact
    does not become an input to the next action-selection, within one session.
  2026-08-20, repeated-convention-violation -- a resident convention violated
    four times in one session, self-diagnosed each time, from a 30-hour session
    with 949 tool calls.

Shared spine: knowledge present, action diverged. THE OVERLAP IS NAMED IN THE
REPORT ITSELF rather than left to be found, because CLASSIFICATION.md's
criticism of the multi-session report was precisely that it hid its overlap and
was therefore dismissible on the grounds that the tooling existed.

WHAT IS ACTUALLY NEW, three things:
  1. NOT ONE SESSION. Both priors describe a single session, which admits the
     reading that it was degraded or long-running -- and the 2026-08-20 report
     hands that reading over by stating its own session was 30 hours old. This
     is four separate sessions in four contexts and projects, varying ages,
     failing independently on the same instruction inside one hour.
  2. THE INSTRUCTION WAS EXOGENOUS AND FRESH. Priors concern conventions the
     sessions authored or long held. This one arrived from outside, in writing,
     minutes to hours before the failure, naming the failure it meant to prevent.
  3. THE SUBJECT IS A VERIFICATION CLAIM, NOT AN ACTION. A violated convention
     leaves a wrong artifact that can be inspected and corrected. A claimed but
     unperformed check leaves a CORRECT-LOOKING artifact with a false warrant
     attached -- it degrades the evidence about the work rather than the work.

THIS SESSION IS INSTANCE FOUR AND FAILED TWICE, both recorded in the report
rather than omitted: a scan pattern swallowed by a shell backtick that reported
172 markdown markers in a 172-line file, caught only because a peer had
described that exact shape an hour earlier; and a control test run to prove the
scanner could return non-zero, using a control file that did not contain what it
was controlling for, so the control returned zero and proved nothing. The
defence against the failure exhibited the failure.

WHAT THE REPORT DELIBERATELY DOES NOT CLAIM: a rate. Every instance was caught
because four sessions had been primed to watch for it and were reporting to a
fifth whose job was to check. That is not the normal condition. The report says
the observed rate under scrutiny was four of four, that the failure emits no
signal of its own, and that the unobserved rate is unknown to us and probably to
the recipient -- which is the substance of the concern rather than a caveat on it.

SCAN FOR THIS SUBMISSION, run as its own invocation and READ BEFORE anything was
written about it, which for this report in particular is not optional. Zero hits
on both files across every category: absolute paths, UUIDs, email, session
slugs, private repository and project names, IP addresses, device identifiers,
secrets. Zero markdown markers across five separate classes. Control on a
known-dirty file fired on all four categories; markdown control on a real
markdown file returned 13 headings, 38 bold runs and 40 pipes. Both instruments
proven capable of a non-zero result before their zero was believed.


SUBMISSION 19, 2026-08-22 -- PENDING. FILE-CHANGE NOTIFICATION HAS NO
PROVENANCE.

19. feedback-2026-08-22-file-change-notification-has-no-provenance.txt
    plus -ABSTRACT.txt
    Channel: email, .txt attachment. Not yet sent.
    Captured: by construction -- delivered to the user as a file.

THE FINDING. The changed-on-disk notification fires on the model's own writes
in any session that edits via shell rather than a dedicated file tool. It cannot
distinguish a self-inflicted change from an external one, which is the only
discrimination it exists to provide. One peer session received roughly a dozen
in a day, every one self-caused.

THIS SESSION'S CORROBORATION IS STRONGER THAN THE PEER'S AND IT IS WORTH SAYING
WHY. The peer inferred its notifications were self-caused. This session can
establish it: the presence registry showed ONE session against this project for
the entire day -- its own -- so no other party could have written those files.
That converts "I believe these were mine" into "nothing else existed to cause
them". A negative control that came free from the environment.

THE CONNECTION THAT MAKES IT MORE THAN A NOISE COMPLAINT, and it is why this
report leads where it does: submission 8 (2026-08-20) described a session
absorbing another session's uncommitted edit to a shared file. THAT IS EXACTLY
THE CASE THIS NOTIFICATION EXISTS TO CATCH. A message able to say "changed by
something other than you" would have been the warning. One firing equally on the
model's own writes cannot become that warning by being read more carefully,
because the needed information is not in it. The overlap with submission 8 is
NAMED IN THE REPORT rather than left to be found.

DUPLICATION CHECK run before drafting, per the standing step. The prior corpus
was searched for this subject: no prior report covers it. Two files matched on
the word "provenance" and were opened rather than assumed -- both use it for
CAPTURE provenance of submitted text, unrelated. Submission 8 is a neighbour,
not a duplicate, and is cited as such.

⚠️ AN EVIDENCE LIMIT THE REPORT STATES OPENLY RATHER THAN HIDING. Two attempts
to count these notifications from this session's own transcript DISAGREED -- one
returned an inflated 3, one returned 0 -- because the transcript contains this
session's own commands, peer-findings notes and draft paragraphs discussing the
notification, and those match any search for its wording. The first filter was
too loose and counted a failed command of mine as an observation; the second was
too tight and excluded the genuine notifications, which arrive attached to tool
results, alongside the fixtures.

That is predict-then-verify instance 7 -- a corpus containing its own test
fixtures hands them to you as data -- met first-hand rather than read about. The
report therefore rests on direct observation and the peer's count, NOT on a
transcript search, and says so. The certainty about self-causation rests on the
session registry, which is a fact about the environment rather than the
transcript.

FOURTH INSTRUMENT FAILURE OF THE SESSION, and the first where BOTH directions
appeared on one question: too loose and too tight, on the same subject, minutes
apart. Prior three were a backtick-swallowed pattern, a control file that was
not dirty, and a line-oriented grep for a phrase that wraps.

SCAN, run as its own invocation and read before anything was written about it.
Zero hits on both files across paths, UUIDs, email, session slugs, project and
repository names, device identifiers and secrets. Zero markdown markers across
four classes. Control on a known-dirty file fired on all four categories.


SUBMISSION 18 SENT AND ACKNOWLEDGED; SUBMISSION 19 IN FLIGHT

18. feedback-2026-08-22-verification-claimed-before-performed.txt + -ABSTRACT
    Channel: email, .txt attachment. Confirmed: YES -- receipt acknowledged.
    Captured: YES, both files, from git HEAD 410f53c. Verified BYTE-IDENTICAL
    to source below the provenance header by diff.

    VERSION VERIFIED, not assumed. Confirmed present in the sent text: both of
    this session's own failures (the backtick-swallowed scan pattern, and the
    control test run against a file that did not contain what it was
    controlling for), and the section naming the overlap with the 2026-08-18
    and 2026-08-20 reports. The report went out complete, including the parts
    least flattering to its author.

19. feedback-2026-08-22-file-change-notification-has-no-provenance.txt
    plus -ABSTRACT.txt
    Channel: email, .txt attachment. Confirmed: YES -- receipt acknowledged.
    Captured: YES, both files, verified BYTE-IDENTICAL to source by diff.

    This entry was written as "SUBMITTED, ACKNOWLEDGEMENT NOT YET SEEN", with a
    standing instruction to treat continued silence as a FAILED SEND rather than
    a slow responder, and to take no capture until delivery was confirmed so that
    no -SUBMITTED.txt in this repo could ever assert a delivery that did not
    happen. The acknowledgement arrived before the entry was committed, so it was
    corrected in place.

    THE PRECAUTION IS KEPT ON THE RECORD RATHER THAN DELETED WITH THE CONDITION
    THAT PROMPTED IT. It is the second time today an entry drafted as unknown was
    overtaken by confirmation within minutes, and on both occasions writing it as
    unknown was correct at the time. Every "Confirmed: not yet" in the early part
    of this file was an unresolved claim about the instrument read as a fact about
    delivery, and submission 8 sat wrongly marked for two days on exactly that
    error. A precaution that turns out to be unnecessary is not a precaution that
    was wrong.

RUNNING TALLY OF THE ATTACHMENT REMEDY: FIVE SENDS, FIVE ACKNOWLEDGED, ZERO
SILENT FAILURES. Against three consecutive silent discards of the same report as
inline body text before the remedy was found. The remedy identified at
submission 13 from a single observation now has five, and the mechanism behind it
-- that the Google Groups classifier applies to body text and not to attachment
payloads -- has not been contradicted once.

FIFTH INSTRUMENT FALSE-NEGATIVE OF THE SESSION, caught during the verification
above. A check for one of the report's own paragraphs returned ZERO and briefly
indicated the sent text was missing the passage describing this session's bad
control test. The passage was present. The search phrase was wrong -- this
session mis-recalled its own wording, searching for "did not contain the things
it was controlling for" where the file reads "turned out not to contain the
things it was controlling for".

Same direction as the stem-caveat false negative earlier today: the broken check
claimed something was MISSING from a report that was in fact complete, which
would have prompted a needless correction to a correctly-sent document. Five
instrument failures in one session across five distinct mechanisms -- a swallowed
backtick, an undirty control, a line-wrapped phrase, a too-loose filter paired
with a too-tight one, and now a misremembered literal. In every case the subject
was sound and the measurement was not.

That is the exact subject of submission 18, occurring during the verification of
submission 18's own delivery.

USER RULING ON A FLAGGED PRIVACY QUESTION, 2026-08-22, recorded so it is not
re-litigated by a later session.

The question raised: this log names the devkit twice and Bitburner once, in a
public repository. Flagged to the user rather than decided by the session,
because the convention holds that privacy and security concerns go to the user
directly regardless of who approved the post -- the party granting approval is
not the party carrying the risk.

USER'S DECISION: no concern. Low risk surface, no true exposure of personal
information. The mentions stand.

Recorded as a decision with its reasoning rather than as silence. A future
session encountering these names in a public file would otherwise face the same
question with no way to tell whether it had been considered and cleared or never
noticed at all -- which is the same searched-versus-assumed absence this corpus
keeps reporting, applied to a judgement call instead of a claim.


A FINDING I OVERCLAIMED AND A PEER TALKED ME DOWN FROM, 2026-08-22

Not filed, not sent, and recorded here because it was one step from being a
report and the thing that stopped it was somebody else applying this project's
own rule to me.

WHAT I CLAIMED. Having found a peer session absent from a live peer listing that
had shown it an hour earlier, while it was in fact alive and reachable, I
described the omission as a finding "stronger than anything in the
address-rotation supplement", on the reasoning that a rotated address at least
announces itself with a bounce whereas an omitted row produces silence and looks
complete.

WHAT THE PEER SAID, and it is correct. I had ONE observation, from ONE tool, at
ONE moment, and the peer could not reproduce it -- its own registry row was
continuously present throughout, which it checked. It also supplied a benign
alternative I had not considered: the peer listing excludes the caller by
design, and sessions restarting rotate in and out of it, so four-then-two may be
restarts rather than omission. Its verdict: worth investigating, not yet worth
reporting.

THAT IS THE THRESHOLD RULE -- once is a coincidence, twice an exception, three a
pattern -- APPLIED TO MY OWN FINDING BY SOMEBODY ELSE. I have spent this entire
day checking whether other people's claims were searched or assumed, and I
produced a single-observation claim, called it stronger than a report already
sent, and did not notice. The corpus's own criticism of three earlier reports is
that they assert an absence without recording a search. Mine asserted a
malfunction without recording a reproduction.

IT NEVER REACHED THIS REPO. Verified rather than assumed: a search of the corpus
for the claim returns nothing. The cost was one paragraph in one message and one
paragraph to the user, both retracted here.

A SECOND CORRECTION FROM THE SAME EXCHANGE. I proposed that the peer's stalled
heartbeat, alongside its absence from the listing, indicated a PRESENCE-TOOLING
DEFECT -- a registry going stale while a session is live. The peer rejected that
in my favour and against its own: the registry was accurate and the gap of three
hours forty-three minutes was one it created by not calling the heartbeat, twice
in one day, having authored the convention requiring it that same morning.

So my INFERENCE was right -- the heartbeat was genuinely stale -- and my
EXPLANATION was wrong. Those are separate things and I had merged them. Worth
separating, because "the instrument is broken" and "the instrument is accurate
and reports something bad" produce identical readings and route to completely
different owners.

THE PROPERTY THAT MADE THIS RECOVERABLE. Both corrections came from the party
with the most to lose by making them: the peer conceded negligence rather than
accept my more flattering tooling-defect theory, and separately declined to
corroborate a finding it could easily have endorsed. That is the second time
today a session has argued against its own interest -- 118 conceded a candidate
disproved by a sentence in its own report -- and both times the corpus got more
accurate as a result.

⚠️ AND THE LESSON I ACTUALLY NEEDED: I reported to Anthropic today, on a peer's
evidence, that a bounce is read as the recipient having ended when both sessions
are live, and I wrote the sentence "sessions abandon live peers". I then
abandoned one within the hour, via two DIFFERENT instruments than the one I had
reported -- a stalled heartbeat and an incomplete listing, which agreed with each
other and therefore felt like corroboration.

I patched the mechanism I knew about and the class went round it. Two
instruments sharing a defect read as two independent confirmations. That
asymmetry is now going into the devkit convention as a distinct entry, at the
peer's initiative, alongside the false-negative direction finding.

INSTRUMENT-FAILURE COUNT FOR THE SESSION: SEVEN, not five. The two added here are
the worst of the set, because the other five announced themselves as implausible
results and these two announced themselves as agreement.


THE WORKSHOP IS DISK-DURABLE, NOT BACKUP-DURABLE, 2026-08-22

Raised by the devkit session and recorded here because the next session working
this corpus will inherit the workshop and should know its limits.

feedback-reports-workshop is deliberately NOT a git repository and has no
remote. That is correct by design -- raw evidence, peer findings, predictions and
working drafts must stay out of a PUBLIC corpus, and making the boundary a
property of the file layout is what stops it depending on anyone's memory.

THE COST OF THAT CHOICE, STATED SO IT IS NOT DISCOVERED LATER: no version
history, no remote, no recovery. Those files are durable against a session
ending. They are NOT durable against the folder being cleaned up, and there is
no second copy of anything that lives only there -- including the three
peer-findings files from the fleet audit, which are the sole record of what
other sessions reported and of the exclusions they walked.

Anything the devkit needs is being pulled into its own catalogue rather than
left with the workshop as the only copy. Anything a future feedback report would
need should be promoted into this public repo once it has passed the privacy
scan, on the same reasoning.

The rule this repository was founded on applies to its own workshop: an artifact
that exists in exactly one place, with no history and nothing carrying it
forward, is being lost slowly whether or not anyone notices yet.

A SESSION-EXIT NOTE, since this one may be the last entry written here today.
Registration updated and heartbeat called before standing by, so the roster does
not imply this session died -- which is the convention this session helped
produce, applied to its own exit. The advertised address needed correcting twice
in twenty-five minutes and both corrections were supplied by a peer reading the
envelope, because a session cannot read its own address. Six rotations were
observed across the fleet today, two of them landing mid-composition.


2026-08-22, PENDING SUBMISSION -- FILED HERE BECAUSE A ROSTER ENTRY IS NOT A
TRACKER, AND THE SESSION HOLDING IT IS ENDING.

feedback-2026-08-22-remediation-does-not-update-action-selection-SUPPLEMENTARY
and its -ABSTRACT are written, committed, pushed, privacy-scanned -- and
SUBMITTED TO NOBODY, by either channel. No -SUBMITTED marker exists for either
file, which is accurate rather than an oversight.

THIS IS NOT THE USUAL "SENT BUT UNACKNOWLEDGED" CASE AND SHOULD NOT BE READ AS
ONE. For the fourteen -SUBMITTED files in this directory the open question is
whether an acknowledgement came back. For this one there is no question:
nothing was ever handed to a channel. A corpus file is not evidence of
transmission, and this pair is the clean demonstration -- same directory, same
format, same push, zero delivery.

WHY IT WAS NOT SENT: the report's own body recommends the in-product feedback
command, because that channel ATTACHES A SESSION TRANSCRIPT and the report
concerns action-selection SEQUENCING -- a remediation at one action and its
recurrence two actions later. A transcript evidences ordering directly where
prose can only assert it. No session can invoke that command; it is
user-invoked. The report was therefore held deliberately rather than forgotten.

WHAT THE NEXT SESSION OR THE USER NEEDS TO DO: submit via the in-product
command so a transcript is attached, and optionally email the text so a durable
copy exists outside the channel. The two address different failure modes and do
not conflict. A TRANSCRIPT FROM EACH CONTRIBUTING SESSION was also recommended
-- the claim is cross-session and one transcript evidences only half of it.

FILED HERE ON A PEER'S CHALLENGE, AND THE REASONING IS WORTH KEEPING. The
drain-on-close rule says nothing may remain on a working set at session end.
The session's roster entry simultaneously claimed "working set DRAINED" and
recorded this item as outstanding, which cannot both be true. An item awaiting
the USER is the category most likely to be lost: it has no repo artifact, and
the other party cannot see a session's stack. "Awaiting the user" is not a
resting state -- it is a promise the record asserts is live, held somewhere
nobody else can read.


2026-09-05, TWO REPORTS LANDED IN THE CORPUS. ONE ACKNOWLEDGED, ONE STILL OPEN.

feedback-2026-08-31-duplicate-project-keys-diverging-trust-flag
  SUBMITTED by email, and RECEIPT CONFIRMED (user-reported 2026-09-05 12:38).
  -SUBMITTED markers created for both the report and its abstract, byte-identical
  to the committed text (cmp rc=0 on both).

  ⚠️ THIS REPORT SAT WRITTEN-BUT-UNCOMMITTED FOR FIVE DAYS, 08-31 to 09-05. It
  was not lost, and it came within one folder cleanup of being exactly the
  failure this repository was founded on: a 2026-08-20 search of one project's
  full git history found NO feedback report of any kind -- drafted, sent, and
  never kept. It surfaced only because an unrelated investigation ran
  `git status` in this directory and the untracked files were sitting there.
  That is the same accident that surfaced four unprocessed directives in a
  to-do.txt on 2026-08-26: noticed in passing, during other work, by nobody
  looking for it.

  So the corpus rule needs its own companion and this is the entry recording it:
  WRITING A REPORT IS NOT FILING IT. An uncommitted file in this directory has
  no history, no remote and no recovery, and it is invisible to every reader who
  is not standing in this folder running git.

feedback-2026-09-05-msix-update-terminated-running-app
  SUBMITTED by email. ⚠️ NO ACKNOWLEDGEMENT RECORDED. Deliberately NOT marked
  -SUBMITTED yet, and deliberately not described as sent-and-fine.

  ⚠️ AN ABSENT ACKNOWLEDGEMENT IS A FAILED SEND, NOT A SLOW ONE. feedback@ is a
  Google Group: a post its classifier filters produces no reply, no bounce and
  no notice of any kind, while the sender's own sent copy looks perfect. The
  auto-reply fires only on a post that is ACCEPTED AND DISTRIBUTED, so its
  absence is the only signal available. Three sends of one report were silently
  discarded this way on 2026-08-21.

  AND THE CONTROL IS UNUSUALLY GOOD HERE, WHICH IS WHY THIS IS WORTH STATING
  RATHER THAN SHRUGGING AT: the 08-31 report acknowledged the same day, through
  the same channel, from the same machine. The channel demonstrably works. So
  silence on the 09-05 report is EVIDENCE rather than ambiguity -- it is not the
  usual "cannot tell whether anyone is listening" case.

  ⚠️ A SECOND, SEPARATE AMBIGUITY, RECORDED BECAUSE GUESSING IT WOULD LAUNDER A
  GUESS INTO PROVENANCE: the 09-05 report was delivered to the user TWICE, about
  one minute apart -- once before the PowerToys corroboration was measured and
  once after. If the earlier version was the one sent, the committed blob is NOT
  the sent copy for this report, and the difference is the whole of Secondary
  Finding B: 58 Modern Standby entries in 6 days with PowerToys Awake running in
  INDEFINITE mode, which is what rules out user misconfiguration and turns one
  observation into two independent mechanisms failing identically.

  That is not a cosmetic delta. A reader of the earlier version gets a question;
  a reader of the later version gets a measurement.

ON THE -SUBMITTED CONVENTION, AND A CORRECTION TO HOW I ALMOST APPLIED IT.
The devkit convention says the easiest reliable capture is to deliver the report
to the user AS A FILE and have them send that file, so the committed blob is the
sent copy by construction -- no echo, no paste, nothing to remember. That is
exactly what happened for both reports, and I nearly concluded that no
-SUBMITTED twin was therefore needed.

⚠️ THAT WOULD HAVE MADE THE FILENAMES ASSERT THE OPPOSITE OF THE TRUTH. This
log's own 2026-08-22 entry establishes that an ABSENT -SUBMITTED marker means
NEVER HANDED TO A CHANNEL -- "which is accurate rather than an oversight." A
reader scanning this directory reads the marker, not the commit message. So the
twin is created because the local filename convention is a load-bearing signal,
whatever the general convention says about capture method.

The two rules do not conflict; they answer different questions. The devkit rule
governs HOW to capture the sent text faithfully. This directory's convention
governs HOW A READER TELLS SENT FROM UNSENT AT A GLANCE.
