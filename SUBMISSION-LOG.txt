SUBMISSION LOG

What was sent, when, by which channel, whether the sent text was captured,
and whether delivery was confirmed.

This file exists because the reports here document a channel that retains no
sent copy. Without a log, "was it truncated?" is unanswerable -- which is
exactly the state that produced the 2026-08-20 report on the feedback tool.

CAPTURED means the exact text sent is stored here as a -SUBMITTED.txt file.
CONFIRMED means delivery was acknowledged by the recipient.


ALL FIVE REPORTS SUBMITTED, 2026-08-20

1.  feedback-2026-08-20-feedback-tool-insufficient.txt
    Channel: in-product feedback tool
    Captured: YES, as -SUBMITTED.txt
    Confirmed: NO acknowledgement of any kind
    Size: 3,249 characters
    Verified identical to the source apart from one leading blank line, so
    nothing was lost at the copy stage.

2.  feedback-2026-08-20-repeated-convention-violation-ABSTRACT.txt
    Channel: in-product feedback tool
    Captured: NO
    Confirmed: NO
    Size: 1,469 characters (source file, presumed content)
    Submitted before the text was copied. What was INTENDED is known from the
    unmodified source; what was transmitted is not. Recorded plainly because
    it is the exact failure this log was created to prevent, occurring on the
    first occasion after the log was proposed -- a weakness in the procedure
    rather than a lapse, since the capture step depends on remembering to
    copy before the irreversible action.

3.  feedback-2026-08-19-multi-session-coordination-tooling-...RECOVERED.txt
    Channel: email, general feedback address
    Captured: YES, in the sent mailbox
    Confirmed: YES, automated reply
    Size: approximately 14,000 characters

4.  feedback-2026-08-18-remediation-does-not-update-...RECOVERED.txt
    Channel: email
    Captured: YES, in the sent mailbox
    Confirmed: YES, automated reply
    Size: approximately 6,200 characters

5.  feedback-2026-08-20-repeated-convention-violation.txt
    Channel: email
    Captured: YES, in the sent mailbox
    Confirmed: YES, automated reply
    Size: approximately 6,600 characters


6.  feedback-2026-08-20-addendum-report-corpus-link.txt
    Channel: in-product feedback tool
    Captured: YES, as -SUBMITTED.txt
    Confirmed: pending
    Size: 2,340 characters
    Verified identical to the source. Sized deliberately under the 3,249
    characters known to pass intact, and supplies the public link that the
    first five reports were all sent without.
    Capture succeeded this time: copy came before submit, which is the
    procedure change made after submission 2 was lost.


7.  feedback-2026-08-20-model-cannot-see-its-own-context.txt
    Channel: email
    Captured: YES, in the sent mailbox
    Confirmed: YES, automated reply
    Size: 3,529 characters
    Sent by email because it exceeds the 3,249 characters known to pass the
    in-product tool intact.


FINDINGS

Email is strictly better than the in-product tool on every axis that was
actually tested.

It retains its own sent copy, so no discipline is required and the capture
cannot be forgotten -- which is precisely how submission 2 was lost. It
acknowledges receipt, so delivery is confirmed rather than assumed. It has no
practical length limit, so the 14,000 character report went whole. And it can
carry a link to this repository for anything longer still.

The in-product tool provided no acknowledgement for either submission made
through it. Whether those two arrived, and in what state, remains unknown and
now probably unknowable.

That asymmetry is worth stating precisely, because it was measured rather
than assumed: four of four email submissions confirmed, zero of three
in-product submissions acknowledged.

On the length limit: a 3,249 character report survived composition and
copying intact through the in-product tool, and a 6,600 character report was
reported as too large for it. The limit sits above 3,249 and at or below
6,600. That bound exists only because one submission was captured, and it is
the only quantitative fact anyone has about this channel.


PROCEDURE

Use email for anything substantial. It captures, confirms, and does not
truncate.

If the in-product tool is used, copy the text FIRST, then submit. The capture
is the fragile step and the irreversible one comes second, which is the wrong
order and the reason submission 2 has no record.

For anything past a few thousand characters, link rather than paste:
https://github.com/stuartsmith78/feedback-reports

ESCALATION PATH, IF FEEDBACK SUBMISSION KEEPS FAILING

The user has stated (2026-08-20) that if FEEDBACK SUBMISSION process failures
continue, they are amenable to adding tooling that gives the model partial
access to email, and already has a specific approach in mind. Not built, and
deliberately not designed here -- recorded so the option is not lost and does
not have to be re-proposed from scratch.

What it would fix, concretely: submission 2 in this log was lost because the
capture step depends on a human remembering to copy before an irreversible
action. Model-side send removes that ordering problem entirely, because the
text is already in hand when the send happens.

TRIGGER, stated as a number so it can actually fire, and SCOPED so it counts
the right thing. Apply this project's own threshold rule -- once is a
coincidence, twice is an exception, three times is a pattern -- to FEEDBACK
SUBMISSION failures only: a report sent without a captured copy, or sent and
unacknowledged where acknowledgement was expected.

The scoping matters. Read as process failures generally, the count would
already be far past three, since this project logs failure patterns of many
kinds. Read as feedback submission specifically, it stands at one. Email
tooling addresses the submission path and nothing else, so that is what its
trigger must count.

Count stands at ONE: submission 2, uncaptured. Submission 6 captured
cleanly under the revised copy-first procedure, so the procedure appears to
hold when followed. Two more and the tooling
question should be raised rather than waited on. A condition phrased as
"if failures continue" never fires, because nobody is counting.




SUBMISSIONS 8 AND 9, 2026-08-20

8.  feedback-2026-08-20-commit-only-absorbed-a-peer-session-edit.txt
    Channel: email
    Captured: BY THE EMAIL CLIENT'S OWN SENT COPY, not independently
      verified here. Email is preferred in this repo's convention precisely
      because it leaves a sent copy; that copy has not been diffed against
      the source file, so "identical" is assumed, not measured.
    Confirmed: not yet
    Size: 8,203 characters (source file)

9.  feedback-2026-08-20-commit-only-absorbed-a-peer-session-edit-ABSTRACT.txt
    Channel: NOT STATED by the user. Recorded as unknown rather than guessed.
    Captured: YES, as -ABSTRACT-SUBMITTED.txt
    Confirmed: not yet
    Size: 1,981 characters
    VERIFIED BYTE-IDENTICAL to the source: 35 lines, diff clean after newline
    normalisation. Nothing was truncated.


WHAT SUBMISSION 9 MEASURES, AND WHAT IT DOES NOT

This is the first entry in this log where a COMPLETE ROUND TRIP was actually
measured rather than assumed. The user sent the text and echoed it back; it
was diffed against the source and matched exactly.

That matters because this repo's founding premise is that the channel retains
no copy of what arrived, that truncation is believed to occur, and that past
truncation is therefore unverifiable and should be assumed. Submission 9 puts
a VERIFIED FLOOR under the channel's capacity at 1,981 characters.

It does not establish a ceiling. One measurement below a limit says nothing
about where that limit sits, and the 1,200-1,500 character target for
abstracts was never a claim about the channel -- it is a claim about what a
reader will actually read. Both remain correct. The assume-truncation posture
stands for anything longer, and the inverted-pyramid rule stands regardless,
because it protects against a reader stopping early as much as a channel
cutting off.

One honest note on this submission's own abstract: at 1,981 characters it is
roughly 30 percent over the 1,200-1,500 target. It was trimmed once, from
2,254, and then stopped rather than cutting the classification line or the
inversion, which are the two things that must survive any clipping. The
overage was flagged to the user before sending rather than shipped quietly.

CAPTURE-FAILURE COUNT, unchanged at ONE (submission 2). Submissions 6, 7 and
9 all captured cleanly. The copy-first procedure continues to hold when
followed. Submission 8 is not counted as a failure -- email retains its own
sent copy by construction -- but it is also not counted as a clean capture,
because nothing here verified it.


SUBMISSION 10, 2026-08-21

10. feedback-2026-08-21-injected-retry-indistinguishable-ABSTRACT.txt
    Sent by email, receipt confirmed. Second report sent by email rather than
    through the in-product tool.

    ** FIRST VERIFIABLE SUBMITTED COPY IN THIS REPOSITORY. ** The user echoed
    the abstract back into the session verbatim immediately before sending, so
    -SUBMITTED.txt is a real capture rather than an assumption. Every prior
    email submission was recorded as "not a failure, but not a clean capture,
    because nothing here verified it". This one is verified.

    The mechanism that produced the capture was accidental -- the user pasted
    the text to confirm it, not to archive it. Worth making deliberate: ASK FOR
    THE ECHO BEFORE SENDING. It costs one message and it closes the gap this
    log has carried since submission 2.

    ** SENT ONE REVISION STALE. ** A correction had been written and delivered
    to the user before submission, and was missed in the handoff; the earlier
    text went out. The corrected abstract is the working copy; the sent text is
    preserved in -SUBMITTED.txt.

    The correction: the original says the UI "injects" the retry text. In fact
    the error card carries a "Try again" BUTTON -- the action is user-initiated
    and only the wording is the product's. That distinction is load-bearing,
    because "injects" invites the dismissal "the user chose to retry, working
    as intended", and the accurate framing has no such answer.

    Amendment written and pending submission:
    feedback-2026-08-21-injected-retry-AMENDMENT.txt

    ** PROCESS FAILURE, AND IT IS A NEW ONE. ** Not a capture failure -- the
    capture succeeded. A DELIVERY-ORDERING failure: a correction existed, was
    committed, was delivered, and the stale version was sent anyway because the
    two crossed. The report's own subject is a message being read without the
    context that would have changed its interpretation, which makes this an
    instance of the defect being reported, occurring inside the reporting of it.

    Remedy, cheap and mechanical: before sending, diff the working copy against
    what is about to go out, or re-request the file. Do not send from a copy
    held open in another window.

CAPTURE-FAILURE COUNT, unchanged at ONE (submission 2). Submission 10 captured
cleanly and verifiably.

STALE-SUBMISSION COUNT, new category, at ONE (submission 10).
